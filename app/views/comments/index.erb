<% if @review %>
    <h1>Comments for <%= @review.title %></h1>
<% else %>
    <h1>All Comments</h1>
<% end %>

<ul>
<% @comments.each do |t| %>
   <li><%= t.user.username %> says "<%= t.content %>" about this review: <%= link_to t.review.title, review_path(t.review_id)%></li>

<% end %>
</ul>